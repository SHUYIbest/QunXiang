# Windows 批处理必须用 CRLF 换行，否则 cmd.exe 行解析错乱
# （中文乱码 / "'xxx' 不是内部或外部命令"）。autocrlf 不可靠，显式锁定。
*.bat text eol=crlf
*.cmd text eol=crlf

# 正式二进制文档与交付附件须随版本保存时使用 Git LFS；运行产物仍应被 .gitignore 排除。
docs/**/*.doc filter=lfs diff=lfs merge=lfs -text
docs/**/*.docx filter=lfs diff=lfs merge=lfs -text
docs/**/*.pdf filter=lfs diff=lfs merge=lfs -text
docs/**/*.ppt filter=lfs diff=lfs merge=lfs -text
docs/**/*.pptx filter=lfs diff=lfs merge=lfs -text
docs/**/*.xls filter=lfs diff=lfs merge=lfs -text
docs/**/*.xlsx filter=lfs diff=lfs merge=lfs -text
docs/**/*.zip filter=lfs diff=lfs merge=lfs -text

# 兼容例外：以下文件在启用通配规则前已作为普通 Git blob 提交，不改写共享历史。
docs/superpowers/specs/云端书库-账号资产与分享复制完整设计.docx -filter -diff -merge
docs/superpowers/specs/云端书库阶段二-对象存储与完整下载设计.docx -filter -diff -merge

# 数据库现场快照：禁用换行符转换，防止损坏二进制 PG 文件
storage/pgdata/** -text
storage/objects/** -text
